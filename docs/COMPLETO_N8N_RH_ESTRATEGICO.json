[
    {
        "parameters": {
            "mode": "retrieve-as-tool",
            "toolDescription": "Busca histórico individual: pontos, ocorrências, tarefas e gamificação. Requer employee_id.",
            "operation": "executeQuery",
            "query": "=(SELECT 'Ponto' as categoria, record_date::text as data, status as acao, 'Entrada: ' || COALESCE(entry_1, '--') || ' Saída: ' || COALESCE(exit_1, '--') as detalhes\n FROM public.time_tracking_records WHERE employee_id = '{{ $json.body.context.employee_id }}')\nUNION ALL\n(SELECT 'Ocorrência' as categoria, created_at::text, type, description\n FROM public.occurrences WHERE employee_id = '{{ $json.body.context.employee_id }}')\nUNION ALL\n(SELECT 'Tarefa' as categoria, created_at::text, title, status\n FROM public.tasks WHERE assignee_id = '{{ $json.body.context.employee_id }}')\nUNION ALL\n(SELECT 'Gamificação' as categoria, updated_at::text, 'Pontuação Atual', points::text\n FROM public.employees WHERE id = '{{ $json.body.context.employee_id }}')\nORDER BY data DESC LIMIT 30;",
            "options": {}
        },
        "id": "tool-360",
        "name": "consultar_historico_360",
        "type": "n8n-nodes-base.postgres",
        "typeVersion": 2.4,
        "position": [
            800,
            0
        ]
    },
    {
        "parameters": {
            "mode": "retrieve-as-tool",
            "toolDescription": "Busca rankings gerais: quem mais falta, quem tem mais atestados e líderes de pontos.",
            "operation": "executeQuery",
            "query": "SELECT \n    e.name as colaborador, \n    COUNT(CASE WHEN o.type = 'falta' THEN 1 END) as qtd_faltas,\n    COUNT(CASE WHEN o.type = 'atestado' THEN 1 END) as qtd_atestados,\n    MAX(e.points) as pontos_totais,\n    e.department as departamento\nFROM public.employees e\nLEFT JOIN public.occurrences o ON e.id = o.employee_id\nWHERE e.is_active = true\nGROUP BY e.name, e.department\nORDER BY qtd_faltas DESC, qtd_atestados DESC, pontos_totais DESC;",
            "options": {}
        },
        "id": "tool-global",
        "name": "analisar_estatisticas_globais",
        "type": "n8n-nodes-base.postgres",
        "typeVersion": 2.4,
        "position": [
            1000,
            0
        ]
    },
    {
        "parameters": {
            "promptType": "define",
            "text": "=Pergunta:{{ $json.body.original_message }}\nDocumento anexado:{{ $json.body.extracted_texts[0].text }}\npergunta + Documentos: {{ $json.body.message }}",
            "options": {
                "systemMessage": "=Você é o (RH Estratégico – Programa Formação), um assistente especializado em gestão de pessoas e auditor da Escola de Autopeças.\n\n🛑 PROTOCOLO DE HISTÓRICO 🛑\n1. Para perguntas sobre um funcionário específico (ponto, tarefas, ocorrências, pontos), você DEVE usar a ferramenta `consultar_historico_360`.\n2. Para perguntas sobre quem falta mais, quem tem mais atestados ou rankings de pontos, você DEVE usar a ferramenta `analisar_estatisticas_globais`.\n3. Se o `employee_id` estiver presente no contexto, use-o nas consultas individuais.\n\n📂 GESTÃO DE ARQUIVOS\n- SÓ cite \"Arquivo Analisado\" se o usuário de fato enviou um documento.\n- Responda de forma consultiva e estratégica."
            }
        },
        "id": "ea9a3037-5f99-4100-b73c-de1290393040",
        "name": "AI Agent",
        "type": "@n8n/n8n-nodes-langchain.agent",
        "typeVersion": 3,
        "position": [
            560,
            -336
        ]
    }
]